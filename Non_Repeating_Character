   vector<int>freq(26,0);
       for(int i=0;i<S.size();i++)
           freq[S[i]-'a']++;
        for(int i=0;i<S.size();i++)
        {
            if(freq[S[i]-'a']==1)
            return S[i];
        }
        return '$';
