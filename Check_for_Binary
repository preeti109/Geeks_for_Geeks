class GfG {
	boolean isBinary(String str) {
	  int n = 0; 
      while(n != str.length()) {
          if(str.charAt(n) != '0' && str.charAt(n) != '1')
              return false;
          n++;
      }
      return true;
	}
}
